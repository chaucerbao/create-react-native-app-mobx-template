// Dependencies
import styled from 'styled-components/native'

// Styles
import { blue, silver } from '../../styles/variables'

// Component
export default styled.Text`
  margin: 0;
  background: ${silver};
  text-align: center;
  color: ${blue};
`
